This is a WIP test repository at the moment.

Run the whole thing with `npm run dev`, or:

- build the code with `npm run build`
- watch the code with `npm run watch`
- run a live-server with the `live-server` cli utility
